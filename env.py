from datetime import datetime, timedelta

DISCORD_BOT_TOKEN="YOUR TOKEN HERE"

API_URL = 'https://file.io/'
EXPIRES =  datetime.now() + timedelta(weeks=0, days=0, hours=0, minutes=10, seconds=0)
MAX_DOWNLOADS = 1
AUTO_DELETE = True